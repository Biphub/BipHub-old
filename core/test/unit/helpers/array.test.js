/* global describe it */
import assert from 'assert'
import arrayUtil from '../../../helpers/array'

describe('Array Utils', () => {
  describe('#toString', () => {
    it('Should return ', () => {
      assert.equal('[1,2,3]', arrayUtil.toString([1, 2, 3]))
    })
  })
})
