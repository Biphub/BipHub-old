/**
 * Post a single bip and register it
 * @param req
 */
const postBip = (req) => {
};

/**
 * Get a single bip by posting queries
 */
const getBip = () => {
};

export default {
  postBip,
  getBip
};
