import actions from './BipEditor.actions'
import mutations from './BipEditor.mutations'
import getters from './BipEditor.getters'

const state = {
  apps: [],
};

export default {
  state,
  mutations,
  actions,
  getters,
}