import actions from './App.actions';
import mutations from './App.mutations';
import getters from './App.getters';

const state = {
  apps: [],
};

export default {
  state,
  mutations,
  actions,
  getters,
};